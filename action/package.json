{
  "name": "ghapp-egress-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that runs the egress agent in background and uploads summary to control-plane",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
